# 🐾 Wild Animal Adventure

An interactive, client-side web app that explores wildlife data. Discover animals and view their physical stats and habitats through a modern, responsive interface.

## 🚀 Features
- **Real-time data fetching** with the JSONbin.io API using `fetch`.
- **Dynamic UI generation** that renders animal cards without page refresh.
- **Visual stat tracking** via animated bars for “Aggressiveness” and “Elusiveness”.
- **Randomized discovery** so each click reveals something new.

## 🛠️ Built With
- HTML
- CSS
- JavaScript (vanilla)

## 🧠 Technical Highlights
- **DOM querying** with `querySelector` for UI targets.
- **DOM manipulation** to inject content and adjust styles.
- **Event handling** using `onclick` to drive the app flow.
- **Data mapping** from JSON to UI components.

## 📂 Project Structure
- index.html — Page markup
- style.css — Styling and animations
- script.js — App logic and data fetching

## ▶️ Run Locally
Open index.html in your browser.